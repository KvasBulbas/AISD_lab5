#include "HashTable.h"


int main()
{
	HashTable obj1(7);
	const double a = (-1.0) * (1.0 - sqrt(5)) / 2.0;

	std::cout << (10 * a * 7);

	/*for (int i = 0; i < 50; i++)
	{
		std::cout << obj1.getHash(18, 2);
	}*/
}